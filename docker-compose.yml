version: '3.8'

services:
  backend1:
    image: nginx:alpine
    ports:
      - "8081:80"

  backend2:
    image: nginx:alpine
    ports:
      - "8082:80"

  backend3:
    image: nginx:alpine
    ports:
      - "8083:80"